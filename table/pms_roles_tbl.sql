
  CREATE TABLE "PMS_ROLES_TBL" 
   (	"ROLE_ID" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"ROLE_NAME" VARCHAR2(100 CHAR), 
	 CONSTRAINT "PMS_ROLES_TBL_PK" PRIMARY KEY ("ROLE_ID")
  USING INDEX  ENABLE
   ) ;

  CREATE OR REPLACE EDITIONABLE TRIGGER "PMS_TRG_ROLES" 
before
insert or update or delete on "PMS_ROLES_TBL"
for each row
begin
        IF INSERTING THEN
        IF :NEW.ROLE_ID IS NULL THEN
            :NEW.ROLE_ID := PMS_SEQ_ROLE.NEXTVAL;
        END IF;
    END IF;
end;
/
ALTER TRIGGER "PMS_TRG_ROLES" ENABLE;